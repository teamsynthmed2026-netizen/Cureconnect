{
  "name": "cure-connect-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build && cp -r dist ../public",
    "start": "node backend/server.js"
  }
}
